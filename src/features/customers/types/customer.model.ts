export interface CustomerModel {
  id: string
  name: string
  email: string
  phones: string[]
  active: boolean
  gender: string
  notes: string
  birthDate: string
  userId: string
  createdAt: string
  updatedAt: string
}
